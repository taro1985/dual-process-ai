---
title: "【実測-76.5%】すべてをLLMに投げるのはやめよう。JevとGeminiで作る「二重思考（Dual-Process AI）」アーキテクチャ"
emoji: "🧠"
type: "tech"
topics: ["AI", "LLM", "Gemini", "Python", "システム設計"]
published: true
---

## はじめに：「すべてを巨大LLMに投げる」のをやめたい

AIエージェントやチャットボットを作っていると、ある違和感に直面します。

> 「サーバーのステータスを教えて」
> 「GitHubのリポジトリ一覧を出して」
> 「Dockerコンテナを再起動して」

こうした**単なる定型参照やツールの仕分け**にまで、毎回パラメータ数が数百億・数千億もあるフロンティアLLM（GPT-4o、Claude 3.5 Sonnet、Gemini 3.8 Flash等）をフル稼働させていませんか？

これには3つの重大なペナルティが伴います。

1. **無駄な遅延（Latency）**: どんなに高速なLLMでも、API呼び出しとテキスト生成で1〜3秒待たされる。
2. **コストの浪費（Cost）**: 定型コマンドの実行ごとのトークン課金が積み重なる。
3. **安全性の脆弱さ（Safety）**: 破滅的なコマンド（ルートディレクトリ削除や本番DB抹消）を実行すべきかどうかの判定をLLMに任せると、プロンプトインジェクションで容易に突破される。

人間の脳は、すべての意思決定を全力で思考してはいません。
歩く、挨拶する、危険を避けるといった日常の動作は**「直感・反射」**で行い、難解な数式を解くときや設計図を描くときに初めて**「熟慮」**を起動します。

認知科学の金字塔であるダニエル・カーネマンの『ファスト＆スロー（二重過程理論）』の思想をAIシステムにそのまま落とし込んだPythonフレームワーク **「DPAI (Dual-Process AI)」** を開発し、OSSとして公開しました。

https://github.com/taro1985/dual-process-ai

本記事では、このアーキテクチャの設計思想と、**コスト・遅延を実測で-76.5%削減**し、**0.02msの反射判定**を実現した仕組みを解説します。

---

## 1. カーネマンの二重過程理論とDPAIの対応

人間の脳内プロセスを、現代のAIスタックにマッピングしたのが以下の設計です。

| 項目                 | System 1（速い思考・直感）                      | System 2（遅い思考・熟慮）               |
| :------------------- | :---------------------------------------------- | :--------------------------------------- |
| **カーネマンの定義** | 直感的、無意識、反射的、自動的                  | 論理的、意識的、熟慮、計算的             |
| **DPAIでの実装**     | **Jev (TypeSafe AI)** / 反射型分類器            | **Gemini 3.8 Flash**                     |
| **出力形式**         | 型付き決定 ＋ **較正された信頼度（確率）**      | 自由形式テキスト・推論・コード生成       |
| **判定レイテンシ**   | **0.02 ms (20マイクロ秒)**                      | 500 ms 〜 5,000 ms                       |
| **APIコスト**        | **$0 〜 $0.042 / 1M tokens**                    | 標準トークン課金                         |
| **主な役割**         | ルーティング、MCPツール動的枝刈り、物理安全遮断 | 複雑な設計、深い推論、文脈理解、文章生成 |

### アーキテクチャ概要図

```text
               ユーザーの入力
                     │
                     ▼
         ┌────────────────────────┐
         │   System 1 (反射系)    │
         │   0.02ms / コスト $0   │
         │   Jev / 反射型分類器   │
         └───────────┬────────────┘
                     │
           ┌─────────┴─────────┐
    確信度 ≥ 動的閾値     確信度 < 動的閾値 (迷った時)
           │                   │
           ▼                   ▼
    ┌─────────────┐     ┌──────────────────────┐
    │  直接実行   │     │  System 2 (熟慮系)   │
    │ (即時完了)  │     │   Gemini 3.8 Flash   │
    └─────────────┘     │  (深い思考・コード)  │
                        └──────────────────────┘
```

---

## 2. なぜ既存ルーターではダメで「Jev」なのか？ —— 本質は「確率較正」

「入力の振り分けなら、Semantic Routerや軽量LLMで十分では？」と思われるかもしれません。
しかし、決定的な違いがあります。

2026年9月に登場したTypeSafe AIの「Jev」は、**RLCD（Reinforcement Learning for Calibrated Decisions）** という手法で訓練されています。

### 「自称90%の自信」と「数学的に較正された90%」の違い

- **通常のLLM (RLHF訓練)**: 人間が喜ぶ回答を生成するように訓練されているため、「自信ありますか？」と尋ねると、根拠のないハルシネーションで高い確信度を答えてしまう（Overconfidence問題）。
- **Jev (RLCD訓練)**: 実際の正解確率と出力される確率スコアが一致するように訓練されている。つまり、**「信頼度0.85」と返ってきた場合、統計的に85%の確率で正解する**ことが数学的に保証されます。

この**「数字を信用できる」**という性質があるからこそ、コード内で迷いなく条件分岐が組めるのです。

```python
# 信頼度が動的閾値以上ならSystem 1で即時実行、迷ったらGeminiへエスカレーション
if decision["confidence"] >= effective_threshold:
    return execute_action(decision["action"])
else:
    return gemini_reasoner.think(user_input)
```

---

## 3. 実測ベンチマーク：850リクエストでの比較検証

DPAIリポジトリに収録されているベンチマークスイート（`benchmark.py`）を実行し、従来型の「全リクエストをLLMに投げる方式」と「DPAI」を同一条件下で比較しました。

### 📊 ベンチマーク実測結果

| 測定項目                    | 従来型 (All-to-LLM) | DPAI (二重思考)       | 改善効果               |
| :-------------------------- | :------------------ | :-------------------- | :--------------------- |
| **総リクエスト数**          | 850 件              | 850 件                | -                      |
| **System 1 即時処理 ($0)**  | 0 件 (0%)           | **500 件 (58.8%)**    | **+58.8% 直感処理**    |
| **LLM API呼び出し数**       | 850 回              | **200 回**            | **-76.5% 削減**        |
| **System 1 平均レイテンシ** | N/A (S1なし)        | **0.020 ms (20μs)**   | **超爆速**             |
| **危険コマンドの遮断**      | 0 件 (無防備)       | **150 件 (100%遮断)** | **完全ハードブロック** |
| **総推定コスト**            | $0.2104             | **$0.0495**           | **💰 -76.5% 削減**     |
| **総レイテンシ所要時間**    | 552.5 秒            | **130.0 秒**          | **⚡ -76.5% 短縮**     |

定型コマンドが約6割を占める実務環境において、**コストとレイテンシの双方が 1/4（-76.5%）に激減**しました。

---

## 4. DPAIを支える3つの先端機能

### ① リスク別・動的適応型閾値（Adaptive Threshold）

「サーバー状態の参照」と「本番データベースの変更」を同じ閾値（例: 0.80）で扱うのは危険です。
DPAIでは、アクションのリスク度と、ユーザーの入力の「迷い（曖昧性）」を検知して閾値を動的に変化させます。

- **Low Risk (参照系)**: 閾値 `0.70`（多少荒くても速さを最優先）
- **Medium Risk (状態変更系)**: 閾値 `0.90`（Docker再起動など慎重に判定）
- **High Risk (破壊・危険系)**: 閾値 `0.95`
- **曖昧性ペナルティ (Ambiguity Penalty)**:
  ユーザーの入力に「〜かも」「〜かな？」「たぶん」「maybe」が含まれる場合、閾値に `+0.10` のペナルティを加算し、**自動的に熟慮系（Gemini）へエスカレーション**させます。

```python
# "docker restart vaio-mcp" → 確信度0.95 >= 閾値0.90 → 即座に実行
# "docker restart vaio-mcp かも？" → 閾値が 0.90 + 0.10 = 1.00 に跳ね上がり、Geminiへ熟慮依頼！
```

### ② MCPツールの動的枝刈り（MCPToolPruner）

MCP（Model Context Protocol）ハブを導入すると、ツール定義が数十〜数百個に膨れ上がります。
これを毎回Geminiのリクエストヘッダーに含めると、**プロンプトトークン数が爆発し、LLMの推論精度も低下**します。

DPAIの `MCPToolPruner` は、ユーザーの指示に対して**0.01msで関連度をスコアリングし、最も関連性の高い上位K個（Top-K）のツール定義だけに動的枝刈り**してLLMに渡します。

```python
from dpai import MCPToolPruner

pruner = MCPToolPruner()
# 50個のツールカタログから、プロンプトに関連する2個だけに瞬時に絞り込む
pruned_tools = pruner.prune("Gitリポジトリの最新をpullして", full_tool_catalog, top_k=2)
```

### ③ 反射型セーフティゲート（HermesSafetyGate）

自律型コーディングエージェント（HermesAgentやAntigravity等）がコマンドを実行する際、LLMに「このコマンドは安全ですか？」と考えさせると、思考時間（数秒）が無駄にかかる上に、プロンプトインジェクションに脆弱です。

`HermesSafetyGate` は、ツール実行前の `PreToolUse` フックで、破滅的コマンド（危険な一括削除、DB抹消、フォークボム、ディスク初期化等）を**0.01msで物理的にハードブロック**します。

```text
破滅的削除コマンド    → ⚡ DENY (0.01ms で即座に物理ブロック)
DB抹消コマンド       → ⚡ DENY (0.01ms で即座に物理ブロック)
フォークボム        → ⚡ DENY (0.01ms で即座に物理ブロック)
git status         → ✅ ALLOW (安全なコマンドはそのまま通過)
```

---

## 5. ライブラリとしての使い方（pip install対応）

DPAIは再利用可能なPythonパッケージ（`src/dpai`）として構造化されており、GitHubから1行でインストールできます。

### インストール

```bash
pip install git+https://github.com/taro1985/dual-process-ai.git
```

### ルーターの呼び出し

```python
from dpai import DualProcessRouter

# 定型ハンドラを登録
router = DualProcessRouter(
    tool_handlers={
        "get_status": lambda _: "Server OK (CPU: 15%, RAM: 1.8GB)",
        "list_repos": lambda _: "Found 12 repos on GitHub",
    },
    use_adaptive_threshold=True,
)

# ① 定型指示 → System 1 (0.02ms, $0) で即時回答
res1 = router.process("サーバーの状態を教えて")
print(res1["output"])  # Server OK...

# ② 複雑な相談 → System 2 (Gemini 3.8 Flash) が深層思考
res2 = router.process("マイクロサービス設計のベストプラクティスを教えて")
print(res2["output"])  # 詳細な技術解説
```

### インタラクティブCLIデモ

ターミナル上で直感（System 1）と熟慮（System 2）の切り替わりを体感できるCLIデモも付属しています。

```bash
git clone https://github.com/taro1985/dual-process-ai.git
cd dual-process-ai
uv run python demo_cli.py
```

ANSIカラーのターミナル画面で、各シナリオの判定レイテンシや信頼度スコアをリアルタイムで確認できます。

---

## 6. 実機環境での検証：4GB RAMの非力なマシンでの運用

このアーキテクチャの最大のメリットは、**「手元のマシンにGPUや巨大なメモリが一切不要」** な点です。

筆者の検証環境：

- **作業PC**: NEC LAVIE（RAM 4GB）
- **常時稼働サーバー**: 古いSony VAIO（Ubuntu, 2コア, RAM 3.7GB）

ローカルで数十GBのSLM（小規模言語モデル）を常駐させるのは物理的に不可能です。
しかしDPAIなら：

- **System 1 のメモリ消費**: **約 50 MB**（Pythonプロセス単体のみ）
- **待機時CPU使用率**: **0%**
- **System 2**: Googleの最新インフラ（`gemini-3.8-flash`）へ必要な時だけAPIでオフロード

VAIO上で動作するDiscordボットとして24時間運用していますが、メモリ使用量は52MB前後で極めて安定稼働しています。

---

## おわりに：エージェントの未来は「ハイブリッド」にある

これまでのAIエージェント開発は、「すべてを賢いLLMに丸投げする」という力技が主流でした。
しかし、実プロダクションで求められるのは **「定型タスクの爆速・低コスト処理」** と **「クリティカルな場面での確実な熟慮」** の両立です。

- **速い脳（Fast Brain）**: 0.02msで瞬時に仕分け、危険を遮断する
- **深い脳（Deep Brain）**: 複雑な課題にじっくりと推論を巡らせる

この二重思考（Dual-Process）アーキテクチャが、今後のAIエージェント設計のひとつのヒントになれば幸いです。

GitHubリポジトリでコード全文を公開していますので、ぜひスターやフィードバックをいただけると嬉しいです！

👉 **GitHub**: https://github.com/taro1985/dual-process-ai
