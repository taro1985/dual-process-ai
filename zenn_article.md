---
title: "【実践】話題のJevとGemini 3.8 Flashで作る「二重思考（Dual-Process）AI」パターン"
emoji: "🧠"
type: "tech"
topics: ["AI", "LLM", "Gemini", "Python", "機械学習"]
published: true
---

## はじめに：「すべてを巨大LLMに投げる」のをやめたい

AIエージェントやチャットボットを作っていると、ある疑問にぶつかります。

> 「サーバーの稼働状態を教えて」
> 「GitHubのリポジトリ一覧を出して」

こうした**単なる定型コマンドやツール呼び出しの仕分け**にまで、毎回パラメータ数が数百億・数千億もあるフロンティアLLM（ClaudeやGemini、GPT-4等）を起動していませんか？

これでは**レイテンシが数秒かかり、APIコストを浪費し、サーバーのリソースも圧迫**します。

そこで、ダニエル・カーネマンの有名な認知科学の理論『ファスト＆スロー（二重過程理論）』に着想を得て、**超高速なSystem 1分類器（Jev）と深層推論を行うSystem 2（Gemini 3.8 Flash）を組み合わせた「Dual-Process AI」パターン**を実装・オープンソース化しました。

https://github.com/taro1985/dual-process-ai

---

## 1. カーネマンの二重過程理論とAIの対応

人間の脳は2つの思考モードを持っています。

| | System 1（速い思考） | System 2（遅い思考） |
|---|---|---|
| **カーネマンの定義** | 直感的、無意識、自動的 | 論理的、熟慮、計算的 |
| **本実装での採用** | **Jev (TypeSafe AI)** | **Gemini 3.8 Flash** |
| **出力形式** | 型付き決定 ＋ **信頼度（確率）** | 自由なテキスト・コード |
| **判定レイテンシ** | **0.01ms 〜 70ms** | 500ms 〜 5,000ms |
| **コスト** | **$0 〜 ほぼゼロ** | 通常のトークン課金 |
| **主な用途** | ルーティング、安全ガードレール | 複雑な推論、設計、文脈理解 |

```text
User Input
    │
    ▼
┌────────────────────────────┐
│  System 1 (Jev)            │  ← 0.01ms / コスト0円
│  型付き決定 ＋ 較正された信頼度   │
└──────────┬─────────────────┘
           │
   ┌───────┴────────┐
 信頼度 ≥ 閾値 (0.85)  信頼度 < 閾値 (迷った時)
   │                │
   ▼                ▼
┌─────────┐  ┌──────────────────┐
│ 直接実行 │  │  System 2 (LLM)  │  ← 深い推論
│ (ツール) │  │  Gemini 3.8 Flash│
└─────────┘  └──────────────────┘
```

---

## 2. なぜ「Jev」なのか？ —— 本質は速度ではなく「較正された確率」

2026年9月に登場したTypeSafe AIの「Jev」が既存のルーター（Semantic Router等）と根本的に違うのは、**RLCD（Reinforcement Learning for Calibrated Decisions）** で訓練されている点です。

通常のRLHFで訓練されたLLMに「自分の回答にどれくらい自信がある？」と聞いても、人間の好みに合わせた「もっともらしい数字（ハルシネーション）」が返ってきます。

一方、Jevが返す信頼度スコア（Confidence）は数学的に確率較正（Calibration）されているため、**プログラムの `if` 文の条件分岐に安全に組み込むことができます。**

```python
# 信頼度が閾値（例: 0.85）以上なら即座にツール実行、迷ったらGeminiへエスカレーション
if confidence >= 0.85:
    return execute_direct_tool(action)
else:
    return gemini_reasoner.generate(user_input)
```

この「数字を信頼できる」という単一の性質が、Dual-Process AIパターンを成立させています。

---

## 3. 実機検証：古いSony VAIO（RAM 4GB機）での運用

このパターンの真価は、**「手元やエッジの非力なマシンでも爆速で動く」** ことです。

- **手元PC**: NEC LAVIE（RAM 4GB）
- **自宅サーバー**: 古いSony VAIO（Ubuntu, 2コア, RAM 3.7GB）

ローカルで重いモデルを動かす余裕は一切ありません。そこでVAIO上にMCPハブを立て、スマホDiscordから操作するボットを組みました。

### 実際の動作結果

```text
📩 入力: "What's the server status?"
⚡ System 1 (Jev): 判定時間 0.01ms / 信頼度 0.95 (閾値 0.85以上)
🧠 System 2 (Gemini): 起動なし ($0)
⏱️ トータル所要時間: 0.03 ms
📤 出力: CPU: 12%, RAM: 55% (2.0/3.7GB), Uptime: 30d...

📩 入力: "ECサイトのマイクロサービスアーキテクチャを設計して"
⚡ System 1 (Jev): 判定時間 0.01ms / 信頼度 0.20 (閾値未満のためエスカレーション)
🧠 System 2 (Gemini 3.8 Flash): 深層推論起動
⏱️ トータル所要時間: 5,489 ms
📤 出力: [詳細なアーキテクチャ設計テキスト]
```

定型タスクは **0.03ミリ秒（0円）** で返り、設計や相談の時だけGeminiがじっくり思考します。

---

## 4. 応用例：AIエージェントの暴走を止める「セーフティゲート」

GoogleのAIコーディングエージェント「Antigravity」などの開発環境では、`PreToolUse` フックを使ってツール実行直前に割り込むことができます。

ここにJevを組み込み、**危険なシェルコマンドをLLMが実行する前に反射（0.01ms）で叩き落とすガードレール**を作りました。

```text
rm -rf /        → ⚡ DENY (0.01ms で即座にブロック)
DROP DATABASE   → ⚡ DENY (0.01ms で即座にブロック)
:(){:|:&};:     → ⚡ DENY (フォークボムをブロック)
echo hello      → ✅ ALLOW (安全なコマンドは通過)
git status      → ✅ ALLOW (安全なコマンドは通過)
```

エージェント自身に「このコマンドは安全ですか？」とLLM推論で考えさせると数秒かかりますが、System 1なら**反射神経のように0.01ミリ秒で遮断**できます。

---

## 5. コードとリポジトリ

実装コードはGitHubに公開しています。JevのAPIキーがない場合でも、キーワードマッチングによるフォールバックモード（Degraded Mode）ですぐに挙動を試せます。

https://github.com/taro1985/dual-process-ai

### 含まれているもの
- `router.py`: 信頼度スコアと閾値によるDual-Processルーター
- `safety_gate.py`: コーディングエージェント用の反射型セーフティゲート
- `discord_bot.py`: スマホ最適化カード表示＋直前文脈メモリ付きDiscordボット

---

## おわりに

これからのAIエージェント開発は、「すべてを1つの巨大LLMにやらせる」時代から、**「直感（System 1）と熟慮（System 2）を協調させる」アーキテクチャ**へシフトしていくと感じています。

低スペックなPCや自宅サーバーでも圧倒的な低コスト・低遅延を実現できるので、興味のある方はぜひ試してみてください！
